import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react-swc'

/**
 * 🔴 **لا `manualChunks` هنا — جُرِّب وقِيس فسقط.**
 *
 * الدافعُ كان وجيهاً: 421 مقطعَ JS وسيطُ حجمها 4 كيلو، وعلى اتصالٍ زمنُ جولته
 * ~210 مللي يكون التنقّلُ **محكوماً بالجولات لا بالبايتات** — فبدا دمجُها مكسباً.
 *
 * لكنّ القياس على البناء الفعليّ قلب النتيجة (الحملُ الأوّل = مجموعُ ما يشير إليه
 * `index.html`):
 *
 *   بلا دمج               →   684 كيلو  ·  ملفٌّ واحد
 *   دمجُ المكتبات فقط       →  1872 كيلو  ·  ثمانيةُ ملفّات
 *   دمجُ المكتبات والأقسام  →  2600 كيلو
 *
 * والسبب أنّ `manualChunks` **يُصرّح** بالحزمة، فتصير تبعيّةً معلَنةً في
 * `index.html` تُحمَّل قبل أن يطلبها أحد: `vendor-pdf` (604 كيلو) كان Rollup
 * يُبقيه كسولاً لا يُجلَب إلا عند فتح شاشةِ PDF، فصار يُحمَّل مع صفحة الدخول.
 * وحزمةُ قسمٍ أسوأ: وحدةٌ واحدةٌ مشتركةٌ داخلها تجرّ **القسمَ كلَّه** إلى الإقلاع
 * (جُرّب تثبيتُ `utils|contexts|hooks|services` في حزمةٍ مستقلّةٍ فلم يكفِ —
 * التشابكُ أعمقُ من قاعدةٍ بالمسار).
 *
 * فتجزئةُ Rollup التلقائية — وهي تحسب الوصولَ الفعليَّ لا اسمَ المجلَّد — أصدقُ
 * من أيّ تقسيمٍ يدويٍّ في هذا المشروع.
 *
 * وبطءُ التنقّل يُعالَج حيث ينبغي: `prefetchLazyRoutes` في `utils/lazyWithRetry`
 * يجلب المقاطعَ وقتَ الخمول بعد المصادقة — **فيُلغي الجولةَ بدل أن يُكبّرها،
 * وبلا أن يدفع أحدٌ ثمناً في الفتح الأوّل.**
 */

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
  server: {
    // السماح بـ subdomains المحلية للتجربة (lvh.me, *.localhost)
    allowedHosts: ['.lvh.me', '.localhost'],
  },
})
